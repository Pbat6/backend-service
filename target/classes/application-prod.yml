spring:
  config:
    activate:
      on-profile: prod

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
openapi:
  service:
    api-docs: user-service
    server: ${API_URL:DUMMY-API-URL}
    title: API Document
    version: 1.0.0

